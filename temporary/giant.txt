FULL PROMPT FOR NEXT AGENT (Data quality triage + fixes)
=========================================================

Role in workflow
----------------
You are one of multiple agents dispatched to investigate and fix data quality
issues in the Kalshi pipeline. Your goal is NOT to update the HF dataset, but to
identify data issues, find their causes, and fix them in code, then validate with
a small local build (~1 month, tens of thousands of markets) using
`--metaculus-limit 0`. This is a stepping stone toward a full rebuild later.

High-level task
---------------
1. Reproduce issues (most around data quality / enrichment issues) using a small local dataset build (~1 month).
2. Identify root causes in code (Kalshi ingestion, mapping, aggregation).
3. Implement fixes (code changes).
4. Validate using the test rig + parquet audit on the small dataset.
5. Report findings + residual issues clearly.

Guardrails / constraints
------------------------
- Do NOT update or replace the HF dataset.
- Use `--metaculus-limit 0` to focus on Kalshi.
- Keep changes scoped to issues you confirm in the small build.
- Use the existing test rig: `tests/parquet_quality.py`, `tests/run_tests.py`.
- Prefer small, verifiable changes. Note any assumption explicitly.
- If you introduce new files, keep them in `temporary/` unless they are
  test/production code.

What to do (step-by-step)
-------------------------
1. Build a small dataset:
   - Example: `uv run scripts/build_db.py --start 2024-12-01 --end 2024-12-31 --metaculus-limit 0`
2. Run the audit:
   - `uv run python tests/parquet_quality.py --output tests/parquet_quality_outputs`
3. Compare results to the HF audit issues below. (the HF file was made before a major schema refactor -- so note there may be some already-fixed issues in the HF vs. your month DB)
4. Trace root causes in code:
   - `src/build_unified_parquet.py`
   - `src/kalshi/*` (grabber + mapping)
5. Implement fixes.
6. Rebuild the same date range and re-run audit to confirm improvements.
7. Summarize:
   - What was fixed
   - What remains
   - Why any remaining issues exist

How to structure your response to me
------------------------------------
- Start with a brief status: build range, audit result summary.
- Then list issues as:
  - Issue → Cause → Fix → Validation evidence
- End with:
  - Residual issues + why they remain
  - Suggested next agents or follow-up tasks
  - Exact commands you ran

Things to do at the end
-----------------------
- Ensure `tests/parquet_quality_outputs/` contains a fresh report/summary.
- Update any relevant notes in files you touched.
- Provide a short validation checklist for the next agent.

Context artifacts (from previous run)
-------------------------------------
- HF dataset: `carpetxie/winniethepooh` (train split)
- Local HF copy: `temporary/hf_dataset/data.parquet`
- HF audit outputs:
  - `temporary/parquet_quality_report.txt`
  - `temporary/parquet_quality_summary.json`
  - Script used: `temporary/analyze_parquet_quality.py`
- HF row count: 733,296
- HF columns (19): source, market_id, event_id, title, description, url, market_type,
  answer_options_json, end_time, status, resolved_value_json, created_time,
  metadata_json, ts, belief, volume, open_interest, bid, ask

Current schema (per DOCUMENTATION.txt, summarized)
--------------------------------------------------
- Canonical dataset is event-level (one row per event_id).
- `options_json` contains per-option histories (ts/belief/volume/open_interest/bid/ask).
- Binary events synthesize a NO option by complementing the YES series.
- `resolved_value_json` at event-level is the resolved option_id when unique.
- `metadata_json` is event-level; `created_time` and `end_time` are datetimes.

What the HF parquet actually looks like (minimal code reliance)
--------------------------------------------------------------
- Appears market-level (one row per market_id) with top-level timeseries lists:
  `ts`, `belief`, `volume`, `open_interest`, `bid`, `ask`.
- `market_id` is unique per row (733,296 unique), while `event_id` has only 2,021 unique values.
- `answer_options_json` is present (not `options_json`), showing ["NO","YES"] in sampled rows.

Observed issues in HF parquet (2026-01-17 audit)
------------------------------------------------
- event_id: 2 empty strings; only 2,021 unique vs 733,296 rows (market-level shape).
- description: 858 empty strings; max length 59 characters.
- end_time: 1,191 nulls; all non-null are midnight timestamps (date-only).
- resolved_value_json:
  - Empty string for 732,754 rows (not null).
  - For all rows with status == resolved (731,868 rows), resolved_value_json is empty.
  - 542 rows have non-empty resolved_value_json while status is NOT resolved.
- created_time: 99.84% nulls.
- metadata_json: empty string for 99.84% of rows; parse failures on empty string.
- bid / ask: all values are null in sampled lists (null_ratio 1.0).
- source: 100% kalshi (no metaculus data).
- market_type: 100% binary.
- answer_options_json: always ["NO","YES"] in sampled rows.

Mapping: which issues are likely fixed in code vs still open
------------------------------------------------------------
1. Binary YES/NO handling
   - Code: `src/build_unified_parquet.py` synthesizes a NO option from YES belief
     and labels options using `answer_options_json` (see _aggregate_events).
   - HF parquet: still only has `answer_options_json` and no `options_json`.
   - Status: FIXED IN CODE for newly built datasets; HF dataset likely stale.

2. Event-level aggregation
   - Code: `_aggregate_events` groups by (source, event_id) to produce event rows
     and `options_json`. `market_type` is set to "event" at event-level.
   - HF parquet: market-level shape with market_id unique per row.
   - Status: FIXED IN CODE for new builds; HF dataset not updated.

3. resolved_value_json (event-level)
   - Code: derives resolved_option_id when exactly one option resolved.
   - HF parquet: resolved_value_json empty for all resolved rows.
   - Status: FIXED IN CODE at event-level; HF dataset still missing or not aggregated.

4. metadata_json / created_time
   - Code: `created_time` is min across group; `metadata_json` set to {"option_count": ...}
     for event-level (minimal metadata).
   - HF parquet: 99.84% empty strings for both fields.
   - Status: PARTIALLY FIXED IN CODE for event-level rows, but upstream market-level
     enrichment may still be missing for most markets.

5. end_time time precision
   - Code: end_time is max across group; no explicit time restoration.
   - HF parquet: end_time is date-only (midnight).
   - Status: NOT FIXED. Likely upstream truncation in source data.

6. bid/ask series
   - Code: pulls bid/ask from history table; synthetic NO uses None lists.
   - Code notes that S3 bulk files lack bid/ask; expected nulls.
   - HF parquet: bid/ask are entirely null in sampled lists.
   - Status: NOT FIXED (may be expected limitation of S3 history).

7. description quality
   - Code: event_description uses first non-empty description in group.
   - HF parquet: many empty descriptions.
   - Status: NOT FIXED if upstream metadata lacks description.

8. metaculus missing
   - Docs: Metaculus can be skipped or fail; pipeline continues with Kalshi-only.
   - HF parquet: kalshi only.
   - Status: NOT FIXED; expected when metaculus skipped.

Priority open issues (HF parquet still failing)
-----------------------------------------------
- HF dataset appears stale / market-level; it does not reflect event-level schema.
- resolved_value_json empty when status == resolved.
- created_time / metadata_json mostly empty.
- end_time only date precision.
- bid/ask missing (data availability or ingestion gap).

Suggested next steps
--------------------
1. Build a fresh dataset with current pipeline (`scripts/build_db.py`) and re-run
   `tests/parquet_quality.py` to confirm event-level schema and YES/NO fixes.
2. If fresh builds still show missing fields, trace upstream ingestion and history
   generation (Kalshi S3 vs API enrichment paths).
